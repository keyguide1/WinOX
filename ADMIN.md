# Admin

Administrative financial actions require a specific adjustment transaction,
reason, authorization, and audit event. Admins cannot type a balance into a
field or bypass ledger rules.

Maintenance mode must keep administrators reachable while safely allowing
already-settled financial transactions to complete. Admin APIs require
separate authorization and stricter rate limits.
